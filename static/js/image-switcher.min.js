const mainImage=document.getElementById('main-image');const textBox=document.getElementById('text-box');const buttons=document.querySelectorAll('.btn');buttons.forEach(button=>{button.addEventListener('click',()=>{const imageSrc=button.getAttribute('data-image');const text=button.getAttribute('data-text');mainImage.src=imageSrc;textBox.textContent=text})});